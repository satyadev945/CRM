package crm.controller;

import org.junit.jupiter.api.Test;

class ExportCustomersTest {

    @Test
    void testExportCustomersClassExists() {
        // This is just a placeholder test to verify the class exists
        // Since the class is commented out in the source code, we're just
        // creating a minimal test to acknowledge its presence
        ExportCustomers exportCustomers = new ExportCustomers();
    }
}