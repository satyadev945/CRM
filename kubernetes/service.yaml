apiVersion: v1
kind: Service
metadata:
  name: test-crm-008-service
  namespace: test-crm-008
  labels:
    app: test-crm-008
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: test-crm-008
